from collections import deque
import sys
sys.stdin = open('input.txt', 'r')
def bfs(n, k):
    q = deque(n)
    while q:
        now = q.popleft()
        for nxt in ()
n, k = map(int, input().split())
bfs(n, k)